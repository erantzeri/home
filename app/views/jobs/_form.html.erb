

	<%= form_for ([@contact, @contact.jobs.build]) do |f| %>

		<%= f.label :title %>
		<p><%= f.text_field :title %></p>
		
		<p><%= f.collection_select :company_id, Company.all, :id, :name %>
		
		<p><%= f.submit %></p>
	
	<% end %>

